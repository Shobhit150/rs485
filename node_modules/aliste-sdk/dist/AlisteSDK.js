import axios from "axios";
import { logAPI } from "./apis/logAPI.js";
export default class AlisteSDK {
    client;
    constructor(config) {
        this.client = axios.create({
            baseURL: config.baseURL,
            headers: {
                "Content-Type": "application/json",
                ...(config.apiKey && { Authorization: `Bearer ${config.apiKey}` })
            },
            timeout: 5000
        });
    }
    async logAPI(payload) {
        return logAPI(this.client, payload);
    }
}
//# sourceMappingURL=AlisteSDK.js.map